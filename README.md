# dati-semantic-wordpress

CMS per schema.gov.it
-test trigger

## Local Development

To start the project locally for testing purposes, run the following command:

```bash
docker-compose up -d
```

This will start a WordPress container on port 8080 and a MySQL container on port 3306.
